{
  "name": "@azure-platform-engineering-lab/lifecycle-controller",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --experimental-strip-types ../tests/controller/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "5.9.3"
  }
}
